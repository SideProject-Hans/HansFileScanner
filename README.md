# Hans File Scanner

跨平台檔案掃描工具，使用 Tauri 2.x + React + TypeScript 建構。

<p align="center">
  <img src="src-tauri/icons/128x128.png" alt="Hans File Scanner" width="128" />
</p>

<p align="center">
  <a href="#功能特色">功能特色</a> •
  <a href="#安裝">安裝</a> •
  <a href="#使用方式">使用方式</a> •
  <a href="#開發">開發</a> •
  <a href="#授權">授權</a>
</p>

## 功能特色

- 🔍 **快速掃描** - 遞迴掃描資料夾及子資料夾中的所有檔案
- 📊 **多種檢視模式** - 樹狀結構、文件、圖片、影片、音訊等分類檢視
- 🔎 **即時搜尋** - 透過搜尋框快速找到特定檔案
- 🏷️ **副檔名篩選** - 下拉選單篩選特定副檔名
- 🗑️ **安全刪除** - 檔案移至資源回收筒（而非永久刪除）
- 📁 **批次複製** - 選擇多個檔案並複製到指定目標資料夾
- 💻 **跨平台** - 支援 Windows、macOS、Linux

## 安裝

### 下載安裝程式

前往 [Releases](https://github.com/yourusername/HansFileScanner/releases) 頁面下載適合您作業系統的安裝程式：

| 平台 | 檔案格式 |
|------|---------|
| Windows | `.exe` (安裝程式) |
| macOS (Intel) | `.dmg` |
| macOS (Apple Silicon) | `.dmg` |
| Linux | `.deb`, `.AppImage` |

### 從原始碼建構

請參考 [開發](#開發) 章節。

## 使用方式

### 掃描資料夾

1. 點擊「選擇資料夾」按鈕
2. 選擇要掃描的目標資料夾
3. 點擊「開始掃描」
4. 等待掃描完成，查看檔案清單

### 檢視模式

- **所有檔案** - 顯示掃描結果中的所有檔案
- **樹狀結構** - 以資料夾階層結構顯示
- **文件** - 僅顯示文件類型（.pdf, .doc, .txt 等）
- **圖片** - 僅顯示圖片類型（.jpg, .png, .gif 等）
- **影片** - 僅顯示影片類型（.mp4, .mkv, .avi 等）
- **音訊** - 僅顯示音訊類型（.mp3, .wav, .flac 等）

### 搜尋與篩選

- **搜尋框** - 輸入關鍵字即時搜尋檔案名稱或路徑
- **副檔名篩選** - 從下拉選單選擇特定副檔名

### 檔案操作

1. 勾選要操作的檔案（可多選）
2. 點擊「全選」可選擇目前檢視中的所有檔案
3. **刪除** - 將選中的檔案移至資源回收筒
4. **複製** - 選擇目標資料夾，將選中的檔案複製過去

## 開發

### 系統需求

- [Node.js](https://nodejs.org/) >= 22.x
- [pnpm](https://pnpm.io/) >= 10.x
- [Rust](https://www.rust-lang.org/) >= 1.75
- 作業系統特定相依套件（見下方）

#### Linux 相依套件

```bash
sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
```

### 安裝相依套件

```bash
# 複製專案
git clone https://github.com/yourusername/HansFileScanner.git
cd HansFileScanner

# 安裝前端相依套件
pnpm install
```

### 開發模式

```bash
# 啟動開發伺服器（熱重載）
pnpm tauri dev
```

### 執行測試

```bash
# 前端測試
pnpm test

# 前端測試（含覆蓋率）
pnpm test:coverage

# Rust 後端測試
cd src-tauri
cargo test
```

### 建構發行版

```bash
# 建構所有平台的安裝程式
pnpm tauri build
```

## 專案結構

```
HansFileScanner/
├── src/                      # React 前端原始碼
│   ├── components/           # React 元件
│   │   ├── file-list/        # 檔案清單元件
│   │   ├── filters/          # 篩選元件
│   │   ├── layout/           # 版面配置
│   │   ├── operations/       # 操作元件（刪除、複製）
│   │   ├── scanner/          # 掃描相關元件
│   │   └── ui/               # shadcn/ui 基礎元件
│   ├── hooks/                # 自訂 React Hooks
│   ├── lib/                  # 工具函式
│   ├── stores/               # Zustand 狀態管理
│   └── types/                # TypeScript 型別定義
├── src-tauri/                # Rust 後端原始碼
│   ├── src/
│   │   ├── commands/         # Tauri 命令
│   │   ├── models/           # 資料模型
│   │   └── scanner/          # 掃描引擎
│   └── icons/                # 應用程式圖示
├── tests/                    # 前端測試
└── specs/                    # 規格文件
```

## 技術堆疊

- **框架**: [Tauri 2.x](https://tauri.app/) + [React 18](https://react.dev/)
- **語言**: TypeScript (前端) + Rust (後端)
- **狀態管理**: [Zustand](https://github.com/pmndrs/zustand)
- **UI 元件**: [shadcn/ui](https://ui.shadcn.com/) + [Radix UI](https://www.radix-ui.com/)
- **樣式**: [Tailwind CSS](https://tailwindcss.com/)
- **測試**: [Vitest](https://vitest.dev/) + [React Testing Library](https://testing-library.com/)

## 授權

MIT License - 詳見 [LICENSE](LICENSE) 檔案